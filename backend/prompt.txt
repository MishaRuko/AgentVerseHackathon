Look in clustering.py. In there I take a bunch of sentences, generate embeddings for them, cluster the embeddings, generate summaries for each cluster, and generate an embedding for each cluster. I'd like you to implement a graph builder now. I want you to do the following:
- Build a cosine similarity matrix for the cluster embeddings.
- Use OSLOM to find groups of related clusters.
- For each cluster group, use an LLM to generate an explanation for why these clusters are likely to be related to each other using the cluster descriptions.
- For each cluster group, combine the cluster description and relationship explanation and generate an embedding for this concatenation.
- Return a dictionary mapping embedding to long description.

The LLM calling should be implemented using strands agents. I have a .env file in this directory, let me know what I need to add to it for strands to work.
